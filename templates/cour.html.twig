{% extends 'base.html.twig' %}

{% block title %}
    {{ cour.nom }}
{% endblock %}


{% block body %}

    <h2>{{ cour.nom }}</h2>
    <span>
        {{ cour.description }}
    </span>
    <p>Semestre {{ cour.semestre.numeroSemestre }}</p>

    <a class="btn btn-primary" href="{{ path('edit_cours',{id:cour.id}) }}"> Edit cours</a>
    <a class="btn btn-info" href="{{ path('accueilCours') }}">Retour aux cours</a>

{% endblock %}


